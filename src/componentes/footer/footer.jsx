import React from 'react'
import { Box, Grid, Hidden, IconButton, Link, Menu, MenuItem, Slide, Typography } from '@material-ui/core';

const Footer = () => {
    return (
        <Grid xs={12} style={{backgroundColor:'#000000'}}>
            <Typography vatiant={'h4'} color='primary' > aca puede ir un footer</Typography>
        </Grid>
    )
}

export default Footer
